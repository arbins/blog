%h1 Listing articles

%table.table.table-striped
  %tr
    %th Title
    %th Text
    %th
  - @articles.each do |article|
    %tr
      %td= article.title
      %td= article.text
      %td
        = link_to 'Show', article_path(article)
        = link_to 'Edit', edit_article_path(article)
        = link_to 'Destroy', article_path(article), method: :delete, data: { confirm: 'Click OK to delete the article' }
